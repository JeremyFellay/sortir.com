{% extends 'base.html.twig' %}

{% block title %}Modifier une sortie{% endblock %}

{% block body %}
    <h1 class="text-center text-primary mt-3 pt-3 display-10 fw-bold">Modifier une sortie</h1>
<div class="container-fluid">
    <div class="row justify-content-center">

        <div class="col-9 text-primary">
    {{ include('sorties/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="justify-content-center text-center">
    <a href="{{ path('app_sorties_index') }}" class="btn btn-lg btn-secondary">Retour à la liste</a>

        <div class="justify-content-center text-center">
    {{ include('sorties/_delete_form.html.twig') }}
    </div>
{% endblock %}
