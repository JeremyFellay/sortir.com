{% extends 'base.html.twig' %}

{% block title %}Hello SortieController!{% endblock %}

{% block body %}
    <h1 class="text-center mt-3 pt-3 display-10 fw-bold">Annuler une sortie</h1>
<div class="container-fluid">
    <div class="row justify-content-center">

        <div class="col-5">
            {{ form_start(AnnulerForm) }}

            {{ form_errors(AnnulerForm) }}

            {{ form_row(AnnulerForm.nom) }}
            {{ form_row(AnnulerForm.dateHeureDebut) }}
            Campus : {{ app.user.campus.nom }}
            {{ form_row(AnnulerForm.lieu) }}
            {{ form_row(AnnulerForm.infosSortie) }}




            {# <div class="justify-content-center text-center">
                <button class="btn btn-lg btn-primary" type="submit">Enregistrer</button>
                <button class="btn btn-lg btn-secondary" type="submit">Publier la sortie</button> #}

            <div class="justify-content-center text-center">
                <a href="{{ path('app_sorties_index') }}" class="btn btn-secondary">Retour</a>
                <button class="btn btn-primary" type="submit" >Confirmer l'annulation</button>
                {{ form_end(AnnulerForm) }}
{% endblock %}